# Synthetic example project

This package contains no real product facts. It exists only to prove that the
same pipeline contracts can be selected with a different project identity and
an isolated `projects/example/state` namespace.

Its six schema files are byte-identical to the reusable contract versions in the
first profile. The registry pins those exact bytes. The smoke suite uses only
synthetic IDs, context, evidence, artifacts, and review actions.
